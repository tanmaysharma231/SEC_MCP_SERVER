async def poll_current_feed(watchlist_ciks: list[str]) -> None:
    """
    TODO: poll EDGAR Atom 'current' feed every 10s, detect new 8-K/10-Q/10-K,
    enqueue download+parse, write to local cache/db for instant pull results.
    """
    pass


